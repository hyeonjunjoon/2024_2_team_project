<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- 타이틀 -->
    <TextView
        android:id="@+id/in_RtnList_titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="일상 더하기"
        android:textColor="#000000"
        android:fontFamily="@font/roboto_bold"
        android:textSize="32dp"
        android:textStyle="bold"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true" />

    <!-- RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/in_RtnList_titleText"
        android:layout_above="@id/in_RtnList_new_Button"
        android:layout_marginTop="16dp" />

    <!-- 플러스 버튼 -->
    <ImageButton
        android:id="@+id/in_RtnList_new_Button"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:background="#F0F0F0"
        android:scaleType="fitCenter"
        android:src="@drawable/ic_plus_icon"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="5dp"
        android:contentDescription="Add New Routine" />

</RelativeLayout>
